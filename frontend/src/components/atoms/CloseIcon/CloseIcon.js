import React from "react";

// Components
import { IoClose } from "react-icons/io5";

const CloseIcon = (props) => {
  return <IoClose className="text-2xl mx-2 cursor-pointer" {...props} />;
};

export default CloseIcon;
