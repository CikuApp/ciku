import React from "react";

function FindRecipesButton() {
  const handleClick = () => {
    // redirect to search results page
  };
  return <button onClick={handleClick}>Find Recipes</button>;
}

export default FindRecipesButton;
